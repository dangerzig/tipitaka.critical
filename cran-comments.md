## Test environments
* local macOS (aarch64-apple-darwin25.0.0), R 4.5.2

## R CMD check results
There were no ERRORs or WARNINGs.

There were 2 NOTEs:

1. New submission:
   This is a resubmission of a previously archived package with all issues
   addressed. The package now includes a full testthat test suite (39 tests).

2. Installed size ~6 MB (data/ subdirectory):
   The package ships a single text dataset containing the complete Pali Canon
   (5,777 text units). This is the minimum data needed for the package to
   function. Compression is set to xz.

## Downstream dependencies

There are currently no downstream dependencies for this package.
